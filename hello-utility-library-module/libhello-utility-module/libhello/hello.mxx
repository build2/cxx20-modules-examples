module;

#include <iosfwd>
#include <string_view>

#include <string>

export module hello;

export namespace hello
{
  __symexport
  void
  say_hello (std::ostream&, const std::string_view& name);
}

// Export implementation details for testing (if you think that's ugly, then
// you should have voted for P1203).
//
export namespace hello
{
  __symexport
  std::string
  format_hello (const std::string_view&);
}
