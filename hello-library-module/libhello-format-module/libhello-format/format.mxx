module;

#include <string>
#include <string_view>

export module hello.format;

export namespace hello
{
  __symexport std::string greeting = "Hello";

  __symexport std::string
  format_hello (std::string_view name)
  {
    return greeting + ", " + std::string (name) + '!';
  }
}
