export module hello;

import std;
export import :format;
import :flags;

export namespace hello
{
  struct properties
  {
    properties (): flags_ (flags::none) {}

  private:
    flags flags_;
  };

  void
  say_hello (std::string_view name, const properties& = {});
}
